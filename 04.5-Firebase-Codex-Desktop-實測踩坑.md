# 04.5 Firebase 資料庫 - Codex Desktop 實測踩坑紀錄

> 實測日期：2026-05-02  
> 環境：Windows PowerShell + Codex Desktop + Firebase CLI  
> 實測結果：Firestore 規則部署成功，並完成新增、讀取、刪除測試

---

## 最終成功狀態

這次實際成功部署到 Firebase 專案：

```text
teacherstudy-b16f3
```

Firestore 資料庫：

```text
projects/teacherstudy-b16f3/databases/(default)
```

規則部署結果：

```text
Deploy complete!
```

資料庫控制測試結果：

```text
CREATE_OK=True
READ_OK=True
DELETE_OK=True
```

測試文件位置：

```text
wordcloud_words/codex_control_test_20260502_084153
```

測試完成後已刪除該文件，沒有留下測試垃圾資料。

---

## 踩坑 1：文件寫的專案名稱不一定是 Project ID

文件裡可能寫：

```text
my-teaching-tools
```

但 Firebase CLI 實際查到的專案可能不同。一定要先查：

```powershell
npx.cmd -y firebase-tools@latest projects:list
```

這次實測只看到：

```text
Project Display Name: teacherstudy
Project ID: teacherstudy-b16f3
```

所以 `.firebaserc` 必須填真正的 Project ID：

```json
{
  "projects": {
    "default": "teacherstudy-b16f3"
  }
}
```

不要把 Firebase Console 的顯示名稱誤當成 Project ID。

---

## 踩坑 2：Windows PowerShell 優先用 npx.cmd

Windows 上直接用 `npx` 可能會碰到 PowerShell `.ps1` 執行政策問題。

建議所有 Firebase 指令都用：

```powershell
npx.cmd
```

例如：

```powershell
npx.cmd --version
npx.cmd -y firebase-tools@latest login
npx.cmd -y firebase-tools@latest projects:list
npx.cmd -y firebase-tools@latest deploy --only firestore:rules
```

---

## 踩坑 3：Codex 不能代跑互動式 Firebase login

如果部署時出現：

```text
Failed to authenticate, have you run firebase login?
```

或：

```text
Cannot run login in non-interactive mode
```

代表 Codex 這邊不能直接完成 Google 瀏覽器登入。

處理方式是請使用者自己開一般 PowerShell，執行：

```powershell
npx.cmd -y firebase-tools@latest login
```

瀏覽器完成 Google 授權後，再回到 Codex 繼續部署。

---

## 踩坑 4：403 通常是 IAM 權限不足或登入錯帳號

這次部署曾遇到：

```text
HTTP Error: 403, Caller does not have required permission to use project
Grant the caller the roles/serviceusage.serviceUsageConsumer role
```

先確認 Firebase CLI 目前登入哪個帳號：

```powershell
npx.cmd -y firebase-tools@latest login:list
```

這次查到的登入帳號是：

```text
kathy0075@gmail.com
```

接著到 Google Cloud IAM，把同一個帳號加入必要角色。

建議角色：

```text
Service Usage Consumer
Firebase Admin
```

如果只要部署 Firestore 規則，也可以用：

```text
Service Usage Consumer
Cloud Datastore Owner
```

權限剛加完可能不會立刻生效，通常等 1 到 5 分鐘再重試。

---

## 本次使用的 Firestore 設定檔

`firestore.rules`：

```js
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /wordcloud_words/{document} {
      allow read, write: if true;
    }

    match /{document=**} {
      allow read, write: if false;
    }
  }
}
```

`firebase.json`：

```json
{
  "firestore": {
    "rules": "firestore.rules"
  }
}
```

`.firebaserc`：

```json
{
  "projects": {
    "default": "teacherstudy-b16f3"
  }
}
```

部署指令：

```powershell
npx.cmd -y firebase-tools@latest deploy --only firestore:rules
```

---

## 部署後一定要做 CRUD 測試

規則部署成功只代表 rules 發布成功，不代表 Codex 已經能控制資料庫。

建議做最小測試：

1. 在允許的 collection 新增一筆測試資料
2. 讀回確認內容正確
3. 刪除該測試資料

這次使用的 collection：

```text
wordcloud_words
```

成功判斷：

```text
CREATE_OK=True
READ_OK=True
DELETE_OK=True
```

---

## 快速排查順序

1. `npx.cmd --version`：確認 Windows 上工具可用。
2. `npx.cmd -y firebase-tools@latest login:list`：確認登入帳號。
3. `npx.cmd -y firebase-tools@latest projects:list`：確認真正 Project ID。
4. 檢查 `.firebaserc` 是否填 Project ID。
5. 如果 403，補 IAM 角色 `Service Usage Consumer`。
6. 部署 `firestore.rules`。
7. 做新增、讀取、刪除測試。

---

## 重要結論

- Firebase Console 顯示名稱和 Project ID 可能不同。
- Windows PowerShell 優先使用 `npx.cmd`。
- Firebase login 要由使用者在一般 PowerShell 互動完成。
- Codex 可以部署 rules，但前提是 CLI 已登入且 IAM 權限足夠。
- 部署成功後，要做 CRUD 實測才算真的可控資料庫。
